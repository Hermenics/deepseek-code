# Services — technical design

Enhanced micro-compaction replaces older oversized read-only tool results while retaining the latest eight/tool context. Full compaction asks the model for a summary with nine exact sections, then rebuilds the transcript. Threshold and feature settings select behavior; an Agent-side failure counter circuit-breaks it. 🟢
