# Skills — technical design

Skill lifecycle mirrors the plugin safety pattern but treats `SKILL.md` as its required contract. The manager validates source/name/path, parses constrained YAML-like frontmatter, installs safely, maintains a registry, and supports update rollback. 🟢

Legacy discovery recognizes the prior `.claude` location while current project skills live under `.deepseek`. 🟢
